{
  "name": "body-to-query-params-server",
  "version": "1.0.0",
  "description": "A simple server that receives a JSON payload, and makes an outbound API request with some of the JSON object properties set as query parameters",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Osmos",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.2.2",
    "body-parser": "^1.20.1",
    "express": "^4.18.2"
  }
}
